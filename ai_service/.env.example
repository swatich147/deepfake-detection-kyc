# AI Service Environment Variables
DEVICE=cpu
MODEL_DIR=./weights
MOCK_INFERENCE=true

# true  = fast mock scores (default, no insightface needed)
# false = CPU opencv heuristics (still no insightface in demo Docker image)
