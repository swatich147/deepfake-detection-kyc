# AI Service — demo Docker build (MOCK_INFERENCE=true default)
# No insightface/torch — avoids g++ compile failures on ARM Mac (aarch64)

fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9

pydantic==2.7.1
pydantic-settings==2.2.1
PyYAML==6.0.1

numpy==1.26.4
opencv-python-headless==4.9.0.80

# For MOCK_INFERENCE=false CPU mode (optional — add librosa if using audio lipsync):
# librosa==0.10.2
# soundfile==0.12.1
